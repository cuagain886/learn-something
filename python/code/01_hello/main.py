"""
═══════════════════════════════════════════════════════════════════
 01_hello —— Python 程序的基本结构
═══════════════════════════════════════════════════════════════════

【本节学什么】
  1. 一个 Python 程序由哪几部分组成：import、函数定义、if __name__ 守卫
  2. Python 的注释写法
  3. 如何运行：
        python main.py              # 直接运行
        python -i main.py           # 运行后进入交互模式（方便实验）
        python -m py_compile main.py  # 仅编译检查语法

【Python 和其他语言的第一印象差异】
  - 用缩进而不是花括号划分代码块——缩进就是语法，不是可选风格
  - 动态类型语言：变量不需要声明类型，运行时才确定
  - 解释型语言，写完直接运行，不需要编译步骤
  - "胶水语言"：生态极广，从 Web、数据科学、AI 到运维自动化
  - Python 之禅：import this —— 社区推崇"优雅、明确、简洁"的代码
"""

# ───────────────────────────────────────────────────────────────
# 1. import 导入：使用标准库或第三方库之前先导入
# ───────────────────────────────────────────────────────────────
# - Python 的导入不需要括号，一行一个（也可以用逗号一行多个，但不推荐）
# - 导入了但没用到 -> 不报错，但 IDE/linter 会警告（Python 不像 Go 那么严格）
# - 惯例：标准库、第三方库、本地包 分组，用空行隔开
import sys       # 系统交互：命令行参数、版本信息、退出等
import platform  # 平台信息：操作系统、CPU 架构、Python 实现
import os        # 操作系统交互：环境变量、文件路径等

# ───────────────────────────────────────────────────────────────
# 2. 注释的两种写法
# ───────────────────────────────────────────────────────────────
# 单行注释：# 开头（最常用）

"""
多行注释/文档字符串（docstring）：三引号包裹。
放在模块/类/函数的第一行就是文档字符串，可以用 help() 查看。
注意：Python 没有 /* */ 块注释语法。
"""


# ───────────────────────────────────────────────────────────────
# 3. 定义函数
# ───────────────────────────────────────────────────────────────
# - def 关键字定义函数
# - Python 没有 public/private 关键字：
#   以 _ 开头 = 约定私有（导入时不会被 from module import * 导出）
#   以 __ 开头 = 名称改编（name mangling），更强的"私有"
#   但这些都只是"约定"，技术上仍可访问 —— Python 信奉"我们都是成年人"
def print_environment_info():
    """打印运行环境信息"""
    print("─────────────────────────────")

    # sys 模块提供解释器相关信息
    print("Python 版本 :", sys.version)           # 如 3.13.3 (main, ...)
    print("Python 实现 :", platform.python_implementation())  # CPython / PyPy
    print("操作系统    :", platform.system())      # Windows / Linux / Darwin
    print("CPU 架构    :", platform.machine())     # AMD64 / arm64
    print("当前目录    :", os.getcwd())

    # sys.argv 是命令行参数列表（类似 Go 的 os.Args）
    # sys.argv[0] 永远是脚本自身的路径
    print("命令行参数  :", sys.argv)

    print("─────────────────────────────")


# ───────────────────────────────────────────────────────────────
# 4. print() —— Python 的打印函数
# ───────────────────────────────────────────────────────────────
def demo_print():
    # print() 默认末尾加换行，多个参数之间加空格
    print("Hello, 世界!")  # Python 3 源码默认 UTF-8，直接支持中文

    # end 参数：控制结尾（默认是 "\n"）
    print("不换行", end="")
    print("，所以这句紧跟在后面")

    # sep 参数：控制多个参数之间的分隔符（默认空格）
    print("用", "逗号", "分隔", sep=", ")

    # f-string 格式化（Python 3.6+，最推荐的格式化方式）
    name = "Pythonista"
    print(f"你好，{name}！")

    # 其他格式化方式（了解即可，优先用 f-string）
    print("你好，{}！".format(name))     # str.format()
    print("你好，%s！" % name)           # % 格式化（C 风格，较旧）


# ───────────────────────────────────────────────────────────────
# 5. if __name__ == "__main__" 守卫
# ───────────────────────────────────────────────────────────────
# 这是 Python 程序的"入口"惯用法：
#   - 直接运行此脚本时 __name__ == "__main__"，下面的代码会执行
#   - 被其他文件 import 时 __name__ 是模块名，不会执行
# ⚠️ 这不是语法要求，而是社区惯例。没有它，代码直接写在全局也能跑，
#     但那样被导入时会产生副作用
if __name__ == "__main__":
    demo_print()
    print()
    print_environment_info()

    # 程序退出
    # Python 脚本执行完最后一行就自然退出，退出码为 0
    # 如果想主动退出（非 0 表示出错）：
    #     sys.exit(1)       # 抛出 SystemExit 异常，finally 块会执行
    #     os._exit(1)       # 立即退出，不执行 finally、不刷新缓冲区（极少用）
    print("程序正常结束")
