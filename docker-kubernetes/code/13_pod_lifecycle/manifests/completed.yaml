apiVersion: v1
kind: Pod
metadata:
  name: completed-demo
  namespace: dk-course
spec:
  restartPolicy: Never
  containers:
    - name: task
      image: alpine:3.22
      command: ["sh", "-c", "echo task-complete; exit 0"]
