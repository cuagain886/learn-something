apiVersion: v1
kind: Service
metadata:
  name: lifecycle-api
  namespace: dk-course
  labels:
    app.kubernetes.io/name: lifecycle-api
    app.kubernetes.io/instance: course
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: lifecycle-api
    app.kubernetes.io/instance: course
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
