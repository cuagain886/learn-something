# MCP 与上下文工程 · 学习总览

> 这是 [INDEX.md](../INDEX.md) **阶段 4** 的展开专题。目标：掌握 Agent 的"信息供给"层——**怎么把工具/数据标准化地接进来（MCP）**，以及**怎么把恰当的信息装进有限的上下文窗口（上下文工程 + 记忆）**。
> 延续 [rag/](../rag/00-RAG学习计划总览.md) 和 [agent-core/](../agent-core/00-Agent核心学习总览.md) 的风格：每篇含原理 / 工程实现 / 使用场景 / 常见坑 / 小结 / 检验清单。

---

## 0. 这个专题讲什么，以及为什么它是 2026 的核心

[agent-core/](../agent-core/00-Agent核心学习总览.md) 解决了"Agent 怎么循环、怎么用工具、怎么规划反思"。但有个贯穿始终的隐患没展开：**Agent 的上下文窗口是有限的，而长任务会产生远超窗口的信息。**

上下文窗口既有限又会被工具观察、历史消息、检索证据和工具定义持续消耗。长任务的关键不是寻找一个笼统的“失败占比”，而是能否观测并区分：目标约束丢失、证据未检索到、摘要失真、陈旧状态回填和无关内容污染。

**上下文工程（Context Engineering）**因此从“写好一句 prompt”升级为“系统化决定：每一步需要哪些信息、以什么优先级和表示形式进入有限窗口，并把哪些状态留在窗口之外”。

这个专题覆盖三件紧密相关的事：

```
            ┌─────────────────────────────────────────────────┐
            │              Agent 的上下文窗口（有限）            │
            │   系统提示 + 工具定义 + 历史 + 检索结果 + 记忆…    │
            └─────────────────────────────────────────────────┘
                 ↑供给               ↑策展              ↑持久化
            ┌─────────┐        ┌──────────────┐    ┌──────────┐
            │  MCP    │        │  上下文工程   │    │  记忆系统 │
            │工具/数据 │        │ 装什么/怎么压 │    │ 跨轮/跨会话│
            │标准接入 │        │              │    │ 记什么取什么│
            └─────────┘        └──────────────┘    └──────────┘
```

- **MCP**：工具和数据源**怎么标准化地连进 Agent**（供给侧）。
- **上下文工程**：在每一步**把哪些信息、怎么压缩后**放进窗口（策展侧）。
- **记忆系统**：哪些信息要**跨轮、跨会话持久化**，何时写入、何时取回（持久化侧）。

三者共同回答一个问题：**模型在每一步到底"看到"什么？** 这决定了 Agent 的可靠性上限。

### 子文档清单

| 编号 | 文档 | 核心内容 | 重要度 |
|------|------|----------|--------|
| 01 | [MCP 模型上下文协议](01-MCP模型上下文协议.md) | 2025-11-25 生命周期、能力协商、双向能力、Streamable HTTP、OAuth audience 与 Tasks 边界 | ⭐⭐⭐⭐⭐ |
| 02 | [上下文工程基础](02-上下文工程基础.md) | token 预算、context manifest、来源/时间/作用域冲突、位置与组件消融 | ⭐⭐⭐⭐⭐ |
| 03 | [Agent 记忆系统](03-Agent记忆系统.md) | working context/checkpoint/长期记忆、双时间语义、写入门、冲突、删除与五层评测 | ⭐⭐⭐⭐⭐ |
| 04 | [上下文管理技巧](04-上下文管理技巧.md) | 完整事务裁剪、有损摘要误差、artifact 引用、缓存正确性、compaction vs checkpoint | ⭐⭐⭐⭐⭐ |
| 05 | [工程实践与反模式](05-工程实践与反模式.md) | 确定性装配器、证据驱动诊断、paired eval、安全数据流与生产数据契约 | ⭐⭐⭐⭐⭐ |

---

## 1. 核心心智模型

### 1.1 上下文窗口是"预算"，不是"仓库" ⭐
不要把上下文窗口当成"能塞多少塞多少"的仓库。它是**稀缺、昂贵、且越满越不准**的预算：
- 塞得越多 → 越贵（按 token 计费）、越慢（首字延迟高）。
- 塞得越多 → **越容易"迷失在中间"**（模型对长上下文中间部分利用率下降）、越容易被无关信息干扰。

> 所以上下文工程的核心动作是**做减法和做策展**：在每一步只放"这一步真正需要的最少信息"，而不是把所有历史和检索结果一股脑倒进去。

### 1.2 MCP 是"标准化的供给"，不是"框架" ⭐
[agent-core/02](../agent-core/02-工具使用ToolUse.md) 讲了 function calling——但每接一个工具都要写定制胶水。**MCP 把"工具/数据源 ↔ Agent"的连接标准化**了（"AI 界的 USB-C"），让任意 Agent 用统一接口接入任意工具。它**不是** Agent 框架，是工具集成层，和 LangGraph 等编排框架**互补**。

### 1.3 记忆复用 RAG，但不是“一个向量库” ⭐
长期记忆会复用 [rag/](../rag/00-RAG学习计划总览.md) 的索引、检索和重排，但更难的是**写入治理与时间语义**：什么值得写、事实何时有效、谁是权威来源、新事实如何 supersede 旧事实、删除如何传播到摘要和 embedding。working context、可恢复 checkpoint、长期用户事实也必须分层存储，不能混成一段聊天摘要。

---

## 2. 学习顺序

```
01 MCP            ← 工具/数据怎么标准化接入（承接 agent-core 的工具使用）
      ↓
02 上下文工程基础  ← 建立"窗口即预算"的核心认知，理解失败根源
      ↓
03 记忆系统        ← 跨轮/跨会话记什么、取什么（记忆即 RAG）
      ↓
04 上下文管理技巧  ← 具体手段：滑动窗口/摘要/卸载/压缩/缓存
      ↓
05 工程实践与反模式 ← 组合落地、评估、避坑
```

- **01（MCP）** 相对独立，是 agent-core 工具使用的自然延伸，可先学。
- **02-04 是一条线**：先懂"为什么上下文是命门"（02），再懂"什么该持久化"（03），再懂"具体怎么管"（04）。
- **05 收口**：把它们组合进真实 Agent，并避开反模式。

---

## 3. 与前序专题的衔接

- **承接 [agent-core/](../agent-core/00-Agent核心学习总览.md)**：那里反复出现的"上下文爆炸""计划放弃（忘了目标）""上下文漂移"等失败，根治手段就在本专题。
- **复用 [rag/](../rag/00-RAG学习计划总览.md)**：记忆的派生索引与候选召回复用 RAG 技术；但 canonical memory 还需要双时间、scope/consent、版本冲突、撤销和删除 lineage，不能等同于向量检索。
- **指向后续**：阶段 5（多智能体编排深入）、阶段 6（评估）、阶段 7（安全）。本专题的 MCP 安全和上下文安全是阶段 7 的前哨。

---

## 4. 本专题核心结论（剧透）

- **上下文窗口是稀缺预算**，上下文工程的核心是"策展 + 做减法"，不是"尽量多塞"。
- **不要用无任务边界的统一失败比例代替诊断**——要分别测量约束保留、检索召回、摘要保真、状态新鲜度和上下文利用率。
- **MCP = 工具/数据的标准化供给层**（host/client/server + Tools/Resources/Prompts 三原语），与编排框架互补，安全靠 host 侧的最小权限。
- **记忆复用 RAG 的读取侧，但增加写入、冲突、时间、隔离、删除和污染恢复问题**；checkpoint 也不等于长期记忆。
- **管理技巧都是有契约的变换**：裁剪不能拆散工具事务，摘要要能回指原文，卸载要有 hash/version，缓存键必须包含安全域。
- **最终产物是可重放的 context manifest**：能回答每个 block 从哪来、为何入选、何时过期、被谁裁掉以及当时模型究竟看到了什么。

---

## 5. 面试追问树

| 层次 | 追问 | 合格回答必须覆盖 |
|---|---|---|
| 定义 | context、durable task state、checkpoint、memory 有何区别？ | 模型可见投影 vs 业务真值 vs 恢复点 vs 跨任务事实 |
| 机制 | 每一步上下文如何确定性装配？ | block schema、预算 ledger、优先级、provenance、manifest/hash |
| 反例 | 为什么“把全部历史塞进去”或“摘要一下”会失败？ | 噪声/位置、约束丢失、有损误差、事务被拆、陈旧状态 |
| 协议 | MCP 与编排/A2A 的边界是什么？ | host/client/server、capability negotiation、tool/resource、Task 实验性、A2A 任务协作 |
| 落地 | 记忆怎样处理冲突、时间与删除？ | valid/system time、write gate、CAS/supersedes、TTL、lineage purge |
| 评测 | 怎样证明压缩/记忆/MCP 工具供给有收益？ | paired context ablation、constraint retention、task state、cost/p95、安全切片 |

---

> 下一步：[01-MCP模型上下文协议](01-MCP模型上下文协议.md) —— 把工具和数据标准化地接进来。
