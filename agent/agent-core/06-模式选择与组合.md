# 06 · 模式选择与组合

> 目标：把前面所有模式收口成一套**可操作的决策框架**。读完能拿到任意需求，判断"该用什么、怎么组合、什么时候别用 Agent"，并避开过度工程的反模式。

---

## 1. 第一性原则：从简单开始，失败驱动地加复杂度 ⭐⭐

这是整个 Agent 工程最重要的一条，2026 业界（尤其 Anthropic）反复强调：

> **每个模式都是为某个具体的失败模式存在的。如果你还没遇到那个失败，就别加那个模式。**

```
                 成本太高、调用太多  →  ReWOO / 减少 LLM 往返
                 任务复杂、老跑偏    →  Planning（+ re-plan gate）
ReAct（起点） ──→  正确性不够        →  Reflection / Reflexion
                 需要不同专长/隔离   →  Multi-Agent
                 （没遇到失败）      →  就用 ReAct，别动
```

每个模式都要付出代价（成本、延迟、复杂度、不确定性）。**加之前先问：它解决的是我已经观察到的失败，还是我臆想的需求？**

---

## 2. 一张总决策树

```
你的任务……

① 步骤完全固定、可预测吗？
   是 → 别用 Agent！用写死的工作流 / 单次 LLM 调用 + RAG（最稳最省）
   否 → ②

② 先用 [单 Agent + ReAct + 好工具]（文档01/02）做一版
   在固定评测集上满足质量/延迟/成本/风险 SLO 吗？
   是 → ✅ 保持简单，并建立回归门禁。
   否 → 用 trace 和分群指标定位失败：③

③ 诊断失败类型：
   ├─ 老跑偏、忘了目标、步骤太多        → 加 [Planning + re-plan gate]（文档04）
   ├─ 答得不对/不全，但有客观评判标准    → 加 [Reflection / Reflexion]（文档03）
   ├─ 调用太多太贵                      → [ReWOO] 或精简工具/上下文
   ├─ 需要截然不同的专长 / 上下文隔离    → [Multi-Agent]（文档05），且先确认单Agent真不行
   └─ 检索不到知识                      → 强化 [RAG]（rag/），或做成 Agentic RAG

④ 每次只改变一个主要变量，做 paired eval / ablation
   没形成 Pareto 改善 → 回滚，而不是继续叠模式
```

> 关键提醒：工具、上下文、模型、编排和环境都可能是根因。必须通过 trace、组件级 oracle 和消融定位，不能把失败先验归因给某一层。

---

## 3. 模式速查表

| 模式 | 解决的失败 | 代价 | 何时上 | 文档 |
|------|-----------|------|--------|------|
| **ReAct** | 路径依赖中间观察 | 多轮延迟、状态与停止风险 | 相对单次调用/固定 workflow 的同预算收益成立 | [01](01-Agent循环与ReAct.md) |
| **Tool Use** | 任务需要模型参数外信息或真实动作 | schema、权限、副作用与故障面 | 已定义工具契约、oracle 与最小权限 | [02](02-工具使用ToolUse.md) |
| **Reflection** | 首次候选可被独立反馈纠正 | 每轮增加评估/修订调用，错误可能相关 | 有可区分候选的外部/隐藏评判信号，边际收益为正 | [03](03-反思模式Reflection.md) |
| **Reflexion** | 反复犯同类错 | 中 | 有反馈信号、需会话内学习 | [03](03-反思模式Reflection.md) |
| **Planning** | 复杂任务跑偏 | 中 | 步骤多、结构清晰、需全局视角 | [04](04-规划模式Planning.md) |
| **ReWOO** | 逐步模型调用成为瓶颈 | 预计划可能因新观察失效 | 依赖较稳定，减少调用的收益超过重规划成本 | [04](04-规划模式Planning.md) |
| **Multi-Agent** | 单 Agent 专长/上下文不够 | 高 | 需不同专长/隔离/并行，且单Agent确实不行 | [05](05-多智能体协作模式.md) |

---

## 4. 模式是可以组合的 ⭐

真实的生产 Agent 往往是多个模式的组合。理解每个模式后，组合是自然的：

```
典型的"复杂任务"组合：
   Planning（先拆解出计划）
      └─ 每个子任务用 ReAct + Tool Use 执行
            └─ 关键产出用 Reflection 自检
                  └─ 若子任务需专长，派给专门的 Multi-Agent worker
   全程：循环控制（步数上限）+ 上下文工程 + trace 可观测
```

例子——一个"深度调研报告" Agent：
1. **Planning**：把"调研 X 行业"拆成"找市场规模 / 找主要玩家 / 找趋势 / 写结论"。
2. 每步用 **ReAct + Tool Use**（搜索、抓网页、RAG 检索）执行；遇意外触发 **re-plan**。
3. 写完结论用 **Reflection** 自评"是否覆盖全部子问题、有无事实依据"。
4. 若某块需要深度专业分析，**Orchestrator-Workers** 派给专门 worker。

> 但还是那句：**从最简组合开始，每加一层都因为你观察到了对应的失败。**

### 4.1 选择模式是多目标决策

比较架构不能只看任务成功率。至少形成向量：

\[
m=(quality, pass^k, cost/success, p95\ latency, risk, human\ rate)
\]

版本 B 只有在不被版本 A 支配时才值得继续：如果 B 更贵、更慢、风险更高且质量没有显著提升，它不是“更高级”，只是更复杂。

### 4.2 从基线到复杂架构的实验协议

```text
E0 单次调用 + 必要检索/工具
E1 单 Agent ReAct
E2 E1 + 显式结构化计划
E3 E2 + 独立 verifier/反思
E4 E3 + 多 Agent 并行（只针对可并行分群）
```

每一级：

1. 固定模型、环境快照、工具和最大预算。
2. 在相同 task IDs 上 paired run，多次重复。
3. 报告分群结果和置信区间，不只报平均值。
4. 检查 trace：提升来自哪里，失败是否转移到另一类。
5. 只有达到预先声明的最小改进且未越过风险门禁才升级。

这能回答面试追问：“你怎么证明多 Agent/反思真的有用，而不是模型随机波动？”

### 4.3 自主性按风险分区

同一系统不需要统一自主等级：

| 动作 | 建议控制方式 |
|------|--------------|
| 搜索/只读分析 | Agent 可动态选择，受预算和来源策略限制 |
| 生成草稿/代码补丁 | 沙箱执行 + verifier，可自动迭代 |
| 更新低风险内部状态 | schema + policy + 幂等 + 后置验证 |
| 转账/删库/外发敏感数据 | 参数绑定审批 + 最小权限 + commit gate |

“这是一个 Agent”不能成为所有动作都自动执行的理由。

---

## 5. 横切关注点（无论用什么模式都要做）

这些不是"模式"，但任何能上线的 Agent 都必须有（详见后续 INDEX 阶段）：

- **循环控制**（[文档 01](01-Agent循环与ReAct.md)）：步数/预算上限、防重复、失败喂回、超限兜底。**没有步数上限的 Agent 不能上线。**
- **上下文工程**（[INDEX 阶段 4](../INDEX.md)）：滑动窗口、摘要、记忆卸载与结构化任务状态——控制上下文污染、约束丢失和长轨迹漂移。
- **评估与可观测**（[INDEX 阶段 6](../INDEX.md)）：全链路 trace + 任务成功率/轨迹质量评估。**每加一个模式都要用评估验证它真的提升了**，否则就是徒增成本。
- **安全护栏**（[INDEX 阶段 7](../INDEX.md)）：最小权限、高风险动作人工确认、防 prompt 注入。模式越复杂、自主性越强，安全越关键。

---

## 6. 反模式（明确要避免的）

| 反模式 | 为什么错 | 正解 |
|--------|----------|------|
| **一上来就多智能体** | 巨大复杂度，单 Agent 多半够 | 先 ReAct，失败再升级 |
| **能用工作流却硬上 Agent** | 步骤固定就别要自主性，又贵又不稳 | 固定流程用写死的工作流 |
| **堆砌所有模式** | 规划+反思+多智能体全上，调试地狱 | 失败驱动，只加需要的 |
| **没评估就加模式** | 不知道有没有用，凭感觉 | 先建评估，用数据决定 |
| **没有步数上限** | 跑飞时无限烧钱 | 必设上限和兜底 |
| **没做归因就认定是模式问题** | 可能是模型、工具、上下文、环境或 oracle | trace + 分群 + 组件消融 |
| **自主性与权限不匹配** | 高自主 Agent 拿着危险权限 | 自主越高，护栏越严，最小权限 |

---

## 7. 一句话决策口诀

> **能不用 Agent 就不用（固定流程用工作流）；要用就从 ReAct + 好工具起步；只在观察到具体失败时，才加对应的那一个模式；每加一层都用评估和 trace 验证；越自主，护栏越严。**

---

## 8. 本章小结

- **第一性原则**：从简单（ReAct）开始，**失败驱动**地加复杂度——没遇到对应失败就别加对应模式。
- 用**总决策树**判断：先问"要不要 Agent"，再"ReAct 够不够"，不够则按失败类型加 Planning / Reflection / ReWOO / Multi-Agent。
- Agent 失败必须做证据化归因；不能预设一定是工具/上下文，也不能默认通过更复杂模式解决。
- 架构选择是质量、可靠性、成本、延迟、风险和人工率的 Pareto 决策。
- 模式**可组合**（Planning 套 ReAct 套 Reflection……），但从最简组合起步。
- 无论什么模式，都要做**循环控制 + 上下文工程 + 评估可观测 + 安全护栏**这些横切项。
- 牢记反模式：过早多智能体、能用工作流硬上 Agent、堆砌模式、没评估就加、没步数上限。

## 9. 检验清单

- [ ] 能背出"失败驱动加复杂度"原则，并对每个模式说出它对应的失败。
- [ ] 拿到一个需求，能用决策树判断该不该用 Agent、用哪个模式。
- [ ] 能解释"为什么大多数 Agent 问题是工具/上下文而非模式"。
- [ ] 能设计一个组合多模式的复杂 Agent，并说明每层为什么加。
- [ ] 能列出 4 个以上反模式并说出正解。
- [ ] 能设计 E0–E4 的单变量升级实验，并解释 paired eval/ablation。
- [ ] 能用 Pareto dominance 判断复杂版本是否值得保留。
- [ ] 能按动作风险分配不同自主等级，而不是给整个系统贴一个等级。

---

## 10. 专题回顾

至此你已走完 **Agent 核心循环与设计模式**专题：
- [01 循环与 ReAct](01-Agent循环与ReAct.md)：引擎——思考/行动/观察循环。
- [02 工具使用](02-工具使用ToolUse.md)：手脚——决定能力上限。
- [03 反思](03-反思模式Reflection.md) / [04 规划](04-规划模式Planning.md) / [05 多智能体](05-多智能体协作模式.md)：三种失败驱动的进阶模式。
- [06 选择与组合](06-模式选择与组合.md)：收口决策框架。

> 回到 [INDEX](../INDEX.md) 继续：**阶段 4（MCP 工具标准化 + 记忆/上下文工程）** 是这个专题的天然下一站；也可结合 [rag/](../rag/00-RAG学习计划总览.md) 把"检索"做成 Agent 的一个工具，搭出 Agentic RAG。

> 面试深挖继续：[07-Agent 形式化模型与决策理论](07-Agent形式化模型与决策理论.md) → [08-搜索、规划与测试时计算](08-搜索规划与测试时计算.md)。
