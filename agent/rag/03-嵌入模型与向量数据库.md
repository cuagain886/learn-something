# 03 · 嵌入模型与向量数据库

> 目标：理解 chunk 如何变成向量、向量如何被高速检索，并掌握嵌入模型和向量库的选型。

---

## 1. 嵌入模型（Embedding Model）

### 1.1 嵌入在做什么

嵌入模型把一段文本编码成一个固定长度的向量（如 768、1024、1536 维）。核心性质（[文档 01](01-RAG基础与核心原理.md) 讲过）：**语义相近的文本，向量也相近**。

RAG 里嵌入用在两处，**必须用同一个模型**：
- 建库时：嵌入每个 chunk。
- 检索时：嵌入用户的 query。

> ⚠️ 铁律：**query 和 chunk 必须用同一个嵌入模型编码**，否则向量不在同一空间，相似度毫无意义。换模型必须**重建整个索引**。

### 1.2 关键概念

- **维度（dimension）**：向量长度。维度高→表达力强但存储/计算贵。常见 384/768/1024/1536/3072。
- **对称 vs 非对称检索**：
  - 对称：query 和文档形态相似（句子找句子）。
  - **非对称：短 query 找长文档**（RAG 的典型情况）。很多模型需要给 query 和 doc 加不同的**前缀指令**（如 bge 系列要求 query 前加 `"为这个句子生成表示以用于检索相关文章："`），不加会明显掉点。**务必看模型文档**。
- **归一化（normalization）**：很多模型输出需归一化后用余弦相似度，否则用内积。

### 1.3 主流嵌入模型选型（2025-2026）

| 模型 | 类型 | 特点 | 适用 |
|------|------|------|------|
| **OpenAI text-embedding-3** (small/large) | 闭源 API | 省心、效果稳、支持降维 | 不想自托管、快速起步 |
| **Cohere embed v3** | 闭源 API | 多语言强、有压缩版 | 多语言、企业 |
| **Voyage** (voyage-3 等) | 闭源 API | 检索质量第一梯队 | 追求质量、有预算 |
| **bge-m3 / bge-large** (BAAI) | 开源 | 中英俱佳、可自托管、bge-m3 支持稠密+稀疏+多向量 | **中文场景首选**、需自托管 |
| **jina-embeddings-v3** | 开源 | 长上下文（8K）、支持 Late Chunking | 长文档、Late Chunking |
| **multilingual-e5** | 开源 | 多语言 | 多语言自托管 |

**选型建议：**
- **中文为主 + 想自托管** → `bge-m3` 或 `bge-large-zh`。
- **图省事 + 英文/多语言** → `text-embedding-3-large` 或 Cohere/Voyage。
- 选型前去 **MTEB / C-MTEB 排行榜**看你的语言和任务（Retrieval 类目）下的得分，别只看综合分。
- **务必在自己的数据上小规模评测**——榜单第一不等于你的场景第一。

### 1.4 Matryoshka（套娃）嵌入

新一代模型（text-embedding-3、bge-m3 等）支持 **Matryoshka 表征**：一个高维向量可以**截断**成低维仍保持大部分效果。好处：可以存全维做精排、用低维做粗筛，灵活权衡存储/速度/精度。

---

## 2. 向量索引：为什么检索能这么快

百万级向量，逐个算相似度太慢（暴力搜索 = 精确但 O(N)）。**ANN（近似最近邻）索引**用少量精度换巨大速度。

### 2.1 HNSW（Hierarchical Navigable Small World）⭐ 当前主流

最常用的 ANN 索引，几乎所有向量库都支持。

**直觉**：把向量组织成一个**多层的"小世界"图**。上层稀疏（少量节点、长连接，用于快速跳到大致区域），下层密集（精细定位）。检索时从顶层入口出发，逐层向下"贪心地"跳向离 query 更近的邻居，最后在底层精确定位。像在地图上先看省级、再市级、再街道。

- **优点**：查询极快、召回率高、支持动态插入。
- **缺点**：**内存占用大**（图结构常驻内存）、构建较慢、删除/更新不如重建干净。
- **关键参数**：
  - `M`：每个节点的连接数。大→召回高但内存/构建成本高。常见 16~64。
  - `ef_construction`：构建时的搜索宽度。大→索引质量高但建得慢。
  - `ef_search`：查询时的搜索宽度。**大→召回高但查询慢**（这是查询时调"质量 vs 速度"的旋钮）。

### 2.2 IVF（Inverted File Index）

先用聚类把向量空间分成若干"桶"（cell），查询时只搜最近的几个桶。

- **优点**：内存比 HNSW 省、构建快。
- **缺点**：召回率通常不如 HNSW；需要训练（聚类）。
- 常与 **PQ（Product Quantization，乘积量化）** 组合成 `IVF-PQ`：把向量压缩成短码，**大幅省内存**，代价是精度下降。十亿级、内存受限时常用。

### 2.3 量化（Quantization）—— 省内存的关键

向量很占内存（百万条 1024 维 float32 ≈ 4GB）。量化用低精度表示换内存：

- **标量量化（SQ）**：float32 → int8，省 4x 内存，精度损失小。**性价比高，推荐默认开**。
- **二值量化（Binary）**：每维压成 1 bit，省 32x，配合重排可用，精度损失较大。
- **乘积量化（PQ）**：分段聚类编码，压缩率极高，十亿级用。

### 2.4 索引选型口诀

- **百万级以内、追求召回、内存够** → HNSW（默认）。
- **内存紧张 / 上亿** → IVF-PQ 或 HNSW + 标量量化。
- 大多数业务（<5000 万向量）：**HNSW + int8 量化**就是甜点配置。

---

## 3. 向量数据库选型（2025-2026）

向量库 = 存向量 + 建索引 + 支持过滤 + 提供检索 API。

### 3.1 主流选型

| 数据库 | 形态 | 特点 | 适用 |
|--------|------|------|------|
| **pgvector** (Postgres 扩展) | 嵌入现有 PG | 复用现有 Postgres，**事务/SQL/元数据一体**，~5000万向量舒适 | **绝大多数团队的最佳起点**；已有 PG 基建 |
| **Qdrant** | 独立(Rust) | **过滤性能最强**、量化好、性价比高 | 需要复杂元数据过滤 + 语义搜索 |
| **Milvus** | 独立(分布式) | 为**超大规模**（十亿级）设计、高并发吞吐强 | 数据量极大、高并发 |
| **Pinecone** | 全托管 SaaS | 免运维、省心 | 不想运维、快速上线 |
| **Weaviate** | 独立 | 内置混合检索、模块化 | 想要开箱即用混合检索 |
| **Vespa** | 独立 | 检索+排序+ML 一体，能力最全但重 | 大型搜索系统、复杂排序 |
| **Chroma** | 嵌入式 | 极轻量、本地开发友好 | 学习、原型、小项目 |
| **Elasticsearch/OpenSearch** | 搜索引擎 | 原生 BM25 + 向量，混合检索方便 | 已有 ES 基建、要混合检索 |

### 3.2 选型决策

```
学习/原型           → Chroma / pgvector
已有 Postgres，<5千万 → pgvector（强烈推荐，少引一个组件）
要强过滤 + 中等规模  → Qdrant
十亿级 / 超高并发    → Milvus
完全不想运维         → Pinecone
已有 ES，要混合检索  → Elasticsearch / OpenSearch
```

> **重要提醒**：很多库都宣称"支持 HNSW 向量搜索"，但**"能跑 HNSW" ≠ "生产可用"**。生产可用还要看：**元数据过滤性能、并发吞吐、增量更新/删除、持久化与备份、水平扩展、监控**。很多团队是在生产压力下才发现选错了库。

### 3.3 不容忽视的能力：带过滤的向量搜索（Filtered Search）

实战中几乎一定会用到"**在满足某些元数据条件的子集里做向量搜索**"（如按租户、按时间、按权限）。这件事比想象的难：
- **先过滤再搜（pre-filter）**：精确，但可能破坏 HNSW 图的连通性，过滤太狠时召回掉点甚至变慢。
- **先搜再过滤（post-filter）**：快，但可能 top-k 过滤后剩不下几个，召回不足。
- Qdrant、Milvus 等在这块做了专门优化。**如果你的场景重度依赖过滤（多租户 SaaS 必然如此），把它作为选型的一等公民。**

---

## 4. 建库工程实践

### 4.1 批量嵌入

- **批处理（batching）**：嵌入 API/模型都支持批量，逐条调用会慢几十倍、贵几倍。
- **限流与重试**：API 嵌入要处理 rate limit、超时重试（指数退避）。
- **成本估算**：百万 chunk × 平均 token 数 × 单价，先算账。降维（Matryoshka）能省存储。

### 4.2 增量更新与删除

知识库不是建一次就完事，文档会增删改。设计时就要想清楚：
- **怎么定位一个文档对应的所有 chunk**？（用 `doc_id` 元数据关联，删文档时按 `doc_id` 批量删 chunk。）
- **更新一个文档** = 删旧 chunk + 插新 chunk。
- HNSW 的删除是"标记删除"，删多了图会退化，定期需要 **rebuild/compact**。
- 详见 [文档 08](08-生产工程化与最佳实践.md) 的增量更新章节。

### 4.3 别忘了同时建 BM25 索引

生产 RAG 几乎都用**混合检索**（[文档 04](04-检索策略-混合检索与重排序.md)），所以建库时除了向量索引，通常还要建一份 **BM25/全文倒排索引**。有的库（ES、Weaviate、Qdrant、Milvus）原生支持稀疏向量/全文，pgvector 可配合 PG 全文检索或额外用 `pg_search`/Elasticsearch。

---

## 5. 常见坑

- **query 和 doc 用了不同嵌入模型 / 忘加 query 前缀** → 检索质量莫名其妙地差。
- **换了嵌入模型但没重建索引** → 新旧向量混在一起，全乱。
- **维度选太高** → 存储和内存爆炸，其实降维后效果差不多。
- **没开量化** → 内存成本居高不下。
- **HNSW `ef_search` 默认值太小** → 召回不足；太大 → 查询慢。要按评估调。
- **过滤场景没测性能** → 上线后发现带过滤的查询又慢又漏。
- **建库时没存 `doc_id`** → 后期无法增量更新/删除单个文档。

---

## 6. 本章小结

- 嵌入模型把文本变向量，**query 和 chunk 必须用同一模型**，换模型要重建索引；注意非对称检索的 query 前缀。
- 中文自托管首选 bge-m3；省心选 OpenAI/Cohere/Voyage；选型前在自己数据上评测。
- **HNSW** 是主流 ANN 索引（快、召回高、吃内存），关键旋钮是 `ef_search`；内存紧张用 IVF-PQ 或量化。
- **标量量化（int8）几乎是免费午餐**，建议默认开。
- 向量库：**pgvector 是多数团队最佳起点**，强过滤选 Qdrant，超大规模选 Milvus，免运维选 Pinecone。
- "能跑 HNSW" ≠ "生产可用"，要看过滤性能、并发、增量更新、备份。
- 建库时一并考虑 **BM25 索引**和**增量更新方案**。

## 7. 检验清单

- [ ] 能解释 HNSW 为什么快、牺牲了什么、`ef_search` 调什么。
- [ ] 知道为什么 query 和 doc 必须同模型，换模型要重建索引。
- [ ] 能根据数据规模/过滤需求/运维能力选出合适的向量库。
- [ ] 理解量化在省内存中的作用和代价。
- [ ] 建库时知道要预留 `doc_id` 以支持增量更新。

---

> 下一步：[04-检索策略-混合检索与重排序](04-检索策略-混合检索与重排序.md) —— 生产级 RAG 的准确率核心。
