/*
═══════════════════════════════════════════════════════════════════
 01_hello —— Go 程序的基本结构
═══════════════════════════════════════════════════════════════════

【本节学什么】
  1. 一个 Go 程序由哪几部分组成：package 声明、import、main 函数
  2. Go 的注释写法
  3. 如何运行：在项目根目录执行
        go run ./01_hello          // 编译并直接运行（不留下可执行文件）
        go build ./01_hello        // 编译生成 01_hello.exe（Windows）
        go vet  ./01_hello         // 静态检查常见错误

【Go 和其他语言的第一印象差异】
  - 没有分号（编译器在换行处自动插入，所以左花括号 { 必须和语句同一行）
  - 没有类（class），但有结构体 + 方法 + 接口（后面章节讲）
  - 编译型语言，编译速度极快，产物是单个静态可执行文件，部署不需要运行时
  - 强制代码风格：gofmt 工具统一格式化，社区没有"花括号放哪"之争
  - 未使用的变量、未使用的 import 都是【编译错误】而不是警告
*/

// ───────────────────────────────────────────────────────────────
// 1. package 声明：每个 .go 文件第一行必须声明自己属于哪个包
// ───────────────────────────────────────────────────────────────
//   - 包（package）是 Go 的代码组织单位，类似 Java 的 package / Python 的模块
//   - 一个目录下的所有 .go 文件必须属于同一个包
//   - 名为 main 的包是特殊的：它会被编译成"可执行程序"，
//     其他名字的包都是"库"，只能被别的包导入使用
package main

// ───────────────────────────────────────────────────────────────
// 2. import 导入：使用标准库或第三方库之前必须先导入
// ───────────────────────────────────────────────────────────────
// - 多个导入用圆括号分组写（Go 的惯用写法，gofmt 会自动整理排序）
// - 导入了但没用到 => 编译错误！这是 Go 保持代码整洁的强制手段
// - 导入路径是字符串；使用时用"包名.函数名"调用，如 fmt.Println
import (
	"fmt"     // 格式化输入输出（fmt = format），最常用的包
	"os"      // 操作系统交互：命令行参数、环境变量、文件等
	"runtime" // 运行时信息：操作系统、CPU 架构、Go 版本等
)

// ───────────────────────────────────────────────────────────────
// 3. main 函数：可执行程序的入口
// ───────────────────────────────────────────────────────────────
//   - main 包中必须有一个 main 函数，程序从这里开始执行
//   - func 是定义函数的关键字；main 函数没有参数、没有返回值
//   - 注意：左花括号 { 必须写在 func main() 同一行，
//     换行写会编译错误（因为编译器会在 ) 后自动补分号）
func main() {
	// ── 3.1 打印输出的三种常用函数 ──────────────────────────
	// Println：打印后自动换行，多个参数之间自动加空格
	fmt.Println("Hello, 世界!") // Go 源码就是 UTF-8 编码，直接支持中文

	// Print：不换行、不加空格（相邻参数都不是字符串时才加空格）
	fmt.Print("不换行")
	fmt.Print("，所以这句紧跟在后面\n") // \n 是手动换行符

	// Printf：格式化打印（类似 C 的 printf），%v 表示"按默认格式打印任意值"
	name := "Gopher"             // := 是短变量声明，下一章详细讲
	fmt.Printf("你好，%v！\n", name) // 输出：你好，Gopher！

	// ── 3.2 注释的两种写法 ─────────────────────────────────
	// 单行注释：// 开头（最常用）
	/*
	   块注释：/* 开头 * / 结尾，可以跨多行。
	   惯例：包顶部的文档注释、临时注释掉大段代码时使用。
	*/

	// ── 3.3 调用本文件中的其他函数 ─────────────────────────
	// 同一个包内的函数可以直接调用，不需要包名前缀
	printEnvironmentInfo()

	// ── 3.4 程序退出 ───────────────────────────────────────
	// main 函数执行完毕程序就正常退出，退出码为 0
	// 如果想主动以非 0 退出码退出（表示出错），用 os.Exit：
	//     os.Exit(1)   // 立即退出，注意：defer（后面章节讲）不会被执行
	fmt.Println("程序正常结束")
}

// ───────────────────────────────────────────────────────────────
// 4. 定义自己的函数
// ───────────────────────────────────────────────────────────────
//   - 函数名小写开头 = 包内私有（只能在 main 包内使用）
//   - 函数名大写开头 = 导出/公开（可以被其他包调用）
//     ⚠️ Go 没有 public/private 关键字，【首字母大小写】就是可见性规则！
//     这条规则适用于函数、变量、常量、结构体、字段……所有标识符
func printEnvironmentInfo() {
	fmt.Println("─────────────────────────────")

	// runtime 包提供编译/运行环境信息
	fmt.Println("操作系统:", runtime.GOOS)        // 如 windows / linux / darwin
	fmt.Println("CPU 架构:", runtime.GOARCH)    // 如 amd64 / arm64
	fmt.Println("Go 版本 :", runtime.Version()) // 如 go1.26.4
	fmt.Println("CPU 核数:", runtime.NumCPU())  // 逻辑 CPU 数量

	// os.Args 是命令行参数切片（类似 Python 的 sys.argv）
	// os.Args[0] 永远是程序自身的路径
	fmt.Println("命令行参数:", os.Args)

	fmt.Println("─────────────────────────────")
}
